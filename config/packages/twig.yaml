twig:
    file_name_pattern: '*.twig'
    date:
        timezone: '%env(APP_TIMEZONE)%'
    globals:
        app_name: PartnerOps
    form_themes:
        - 'form/theme.html.twig'

when@test:
    twig:
        strict_variables: true
